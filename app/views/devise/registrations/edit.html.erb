<div class="container">
  <div class="row">
    <div class="col-md-10 mx-auto my-5" style="background-color: #2836a5; padding: 2rem; border-radius: 2rem;">
      <h2 style="color:white">Edit <%= resource_name.to_s.humanize %></h2>
      <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
        <%= f.error_notification %>

        <div class="form-inputs">
          <%= f.input :email,
                      required: true,
                      label_html: { style: "color: white;" },
                      autofocus: true %>
          <%= f.input :name,
                      required: true,
                      label_html: { style: "color: white;" },
                      autofocus: true%>
          <%= f.input :last_name,
                      required: true,
                      label_html: { style: "color: white;" },
                      autofocus: true%>
          <%= f.input :description,
                      as: :text,
                      label_html: { style: "color: white;" },
                      input_html: { rows: 5 },
                      required: true%>
          <%= f.input :photo,
                      as: :file,
                      label_html: { style: "color: white;" },
                      required: true %>

          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
          <% end %>

          <%= f.input :password,
                      label: "Change password",
                      hint: "leave it blank if you don't want to change it",
                      hint_html: { style: "color: white;" },
                      label_html: { style: "color: white;" },
                      required: false,
                      input_html: { autocomplete: "new-password" } %>
          <%= f.input :password_confirmation,
                      label_html: { style: "color: white;" },
                      required: false,
                      input_html: { autocomplete: "new-password" } %>
          <%= f.input :current_password,
                      hint: "type in password to confirm changes",
                      hint_html: { style: "color: white;" },
                      label_html: { style: "color: white;" },
                      required: true,
                      input_html: { autocomplete: "current-password" } %>
        </div>

        <div class="form-actions mb-5">
          <%= f.button :submit, "Update", class:"btn", style:"background-color: #ff7c00; color: white;" %>
        </div>
      <% end %>

      <div class="d-flex justify-content-between">
        <%= button_to "Delete my account", registration_path(resource_name), data: { turbo_method: :delete, turbo_confirm: "Are you sure?", turbo_frame: '_top' }, method: :delete, class:"btn", style:"background-color: #d70c99; color: white;" %>
        <%= link_to "cancel", :back, class:"btn", style:"background-color: #d70c99; color: white;" %>
      </div>
    </div>
  </div>
</div>
